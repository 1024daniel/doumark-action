# .github/workflows/douban.yml
name: douban
on: 
  workflow_dispatch:
  schedule:
  - cron: "30 * * * *"

permissions:
  contents: write

concurrency:
  group: douban-sync
  cancel-in-progress: false

jobs:
  douban:
    name: Douban mark data sync
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: movie
      uses: lizheming/doumark-action@master
      with:
        id: x1024daniel
        type: movie
        status: done
        format: csv
        dir: ./data/douban

    - name: book
      uses: lizheming/doumark-action@master
      with:
        id: x1024daniel
        type: book
        status: done
        format: csv
        dir: ./data/douban

    - name: music
      uses: lizheming/doumark-action@master
      with:
        id: x1024daniel
        type: music
        status: done
        format: csv
        dir: ./data/douban

    - name: Cache posters and deduplicate CSV files
      env:
        DOUBAN_ID: x1024daniel
      run: ruby --disable-gems scripts/process_douban_csv.rb
  
    - name: Commit
      uses: EndBug/add-and-commit@v8
      with:
        message: 'chore: update douban data'
        add: './data/douban'
